import { RequiredCheckbox } from "../components";
import { TimeSettingsProps } from "./types";

const Settings = (props: TimeSettingsProps) => {
  return <RequiredCheckbox {...props} />;
};

export default Settings;
