import { Rating } from "@mui/material";
import { RatingPreviewProps } from "./types";

const Preview = ({ question }: RatingPreviewProps) => {
  return <Rating disabled />;
};

export default Preview;
