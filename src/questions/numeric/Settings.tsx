import { RequiredCheckbox } from "../components";
import { NumericSettingsProps } from "./types";

const Settings = (props: NumericSettingsProps) => {
  return <RequiredCheckbox {...props} />;
};

export default Settings;
