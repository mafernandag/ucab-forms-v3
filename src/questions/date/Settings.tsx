import { RequiredCheckbox } from "../components";
import { DateSettingsProps } from "./types";

const Settings = (props: DateSettingsProps) => {
  return <RequiredCheckbox {...props} />;
};

export default Settings;
