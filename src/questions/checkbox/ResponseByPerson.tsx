import { CheckboxResponse } from "./components";
import { CheckboxResponseByPersonProps } from "./types";

const ResponseByPerson = (props: CheckboxResponseByPersonProps) => {
  return <CheckboxResponse {...props} />;
};

export default ResponseByPerson;
