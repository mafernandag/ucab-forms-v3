import { CheckboxResponse } from "./components";
import { CheckboxResponseByQuestionProps } from "./types";

const ResponseByQuestion = (props: CheckboxResponseByQuestionProps) => {
  return <CheckboxResponse {...props} />;
};

export default ResponseByQuestion;
