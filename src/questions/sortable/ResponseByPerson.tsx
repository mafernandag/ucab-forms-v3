import { SortableResponse } from "./components";
import { SortableResponseByPersonProps } from "./types";

const ResponseByPerson = (props: SortableResponseByPersonProps) => {
  return <SortableResponse {...props} />;
};

export default ResponseByPerson;
