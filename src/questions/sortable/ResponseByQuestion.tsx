import { SortableResponse } from "./components";
import { SortableResponseByQuestionProps } from "./types";

const ResponseByQuestion = (props: SortableResponseByQuestionProps) => {
  return <SortableResponse {...props} />;
};

export default ResponseByQuestion;
